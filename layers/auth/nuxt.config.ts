// https://nuxt.com/docs/api/configuration/nuxt-config
export default defineNuxtConfig({
  // Configuración específica de la layer auth
  // Aquí puedes agregar módulos, plugins, etc. específicos de autenticación
})
